/**
 * Centralized Configuration
 *
 * Single import point for all application configuration.
 * Every module should import config from here, not from individual files.
 *
 * @module config
 */

export { env } from './env'
export type { Env } from './env'
